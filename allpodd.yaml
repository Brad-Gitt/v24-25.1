# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-4.9.3
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: "2026-02-03T12:22:57Z"
  labels:
    # start: nye labels lagt til (person 4)
    app: allpodd
    tier: frontend
    # slutt: nye labels lagt til (person 4)
  name: allpodd
spec:
  ports:
  - name: "80"
    nodePort: 31708
    port: 80
    targetPort: 80
  - name: "81"
    nodePort: 30499
    port: 81
    targetPort: 81
  selector:
    app: allpodd
  type: NodePort
---
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2026-02-03T12:22:57Z"
  labels:
    # start: nye labels lagt til (person 4)
    app: allpodd
    tier: backend
    # slutt: nye labels lagt til (person 4)
  name: allpodd
spec:
  containers:
  - env:
    - name: TERM
      value: xterm
    image: localhost/app:latest
    imagePullPolicy: Never
    name: app
    ports:
    - containerPort: 80
    - containerPort: 81
    stdin: true
    tty: true
  - env:
    - name: TERM
      value: xterm
    image: localhost/bidrag-db:latest
    imagePullPolicy: Never
    name: bidrag-db
    stdin: true
    tty: true
    # start: volumeMounts lagt til (person 4)
    volumeMounts:
    - mountPath: /var/www
      name: bidrag-volume
    # slutt: volumeMounts lagt til (person 4)
  - env:
    - name: TERM
      value: xterm
    image: localhost/pseudonym-db:latest
    imagePullPolicy: Never
    name: pseudonym-db
    stdin: true
    tty: true
    # start: volumeMounts lagt til (person 4)
    volumeMounts:
    - mountPath: /var/www
      name: pseudonym-volume
    # slutt: volumeMounts lagt til (person 4)
  - env:
    - name: TERM
      value: xterm
    image: localhost/web:latest
    imagePullPolicy: Never
    name: web
    stdin: true
    tty: true
  # start: volumes lagt til (person 4)
    volumes:
    - name: bidrag-volume
      persistentVolumeClaim:
        claimName: bidrag-pvc
    - name: pseudonym-volume
      persistentVolumeClaim:
        claimName: pseudonym-pvc
    # slutt: volumes lagt til (person 4)